import styles from './../styles/Headquarters.module.scss';

const Headquarters = function () {
    return (
        <section className={styles.section}>
            <div className={styles.wrapper}></div>
        </section>
    );
};

export default Headquarters;
